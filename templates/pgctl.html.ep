<div class="pgctl">
<ul>
% for my $device (sort keys %{$devices}) {
<li><a class="<%= $devices->{$device}->{status} %>"
% if ($devices->{$device}->{access} eq 'rw') {
href="/pgctl/toggle/<%= $device %>"
% }
><%= $device %></a></li>
% }
</ul>
</div> <!-- pgctl -->
